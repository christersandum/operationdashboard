/* ============================================================
   dataProvider.js — DataProvider factory
   Only the local (offline) provider is available.
   ============================================================ */

export { createLocalProvider, listLocalOperations } from './localProvider';
